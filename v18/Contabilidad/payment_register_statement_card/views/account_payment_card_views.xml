<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_payment_form_prs_card" model="ir.ui.view">
        <field name="name">account.payment.form.prs.card</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="after">
                <!-- Campo técnico necesario para controlar visibilidad -->
                <field name="prs_card_provider_id" invisible="1"/>

                <field name="prs_card_id"
                       readonly="state != 'draft'"
                       options="{'no_create': True}"
                       invisible="not prs_card_provider_id"/>
                <field name="prs_installment_id"
                       readonly="state != 'draft'"
                       options="{'no_create': True}"
                       invisible="not prs_card_id"/>
            </xpath>
        </field>
    </record>

</odoo>
