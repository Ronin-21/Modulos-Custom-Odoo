<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Checkbox 'Es Gasto Vario' en el formulario de pagos (Proveedores > Pagos) -->
    <record id="view_account_payment_form_prs_misc_expense" model="ir.ui.view">
        <field name="name">account.payment.form.prs.misc.expense</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='memo']" position="after">
                <field name="prs_is_misc_expense" invisible="payment_type != 'outbound' or partner_type != 'supplier'"/>
            </xpath>
        </field>
    </record>
</odoo>
